# Copie para `.env` e ajuste. `.env` é ignorado pelo git (segredos fora do código).

# --- Banco (Postgres local via docker-compose) ---
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5433/igni

# Banco de testes (a suíte de integração/RLS aplica as migrations aqui).
TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5433/igni_test

# --- Supabase (preenchido na Wave 3 / deploy) ---
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=

# --- Segurança (configurável, RNF-SEC-05) ---
# AUTH_MAX_LOGIN_ATTEMPTS=5
